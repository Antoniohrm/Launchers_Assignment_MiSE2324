function y = f(p_guess,exhaust,strcoeff,deltav)
y = (exhaust(1)*log((1+p_guess*exhaust1)/(p_guess*exhaust1*strcoeff1))) + ...
    (exhaust(2)*log((1+p_guess*exhaust2)/(p_guess*exhaust2*strcoeff2))) + ...
    (exhaust(3)*log((1+p_guess*exhaust3)/(p_guess*exhaust3*strcoeff3))) - deltav;
end
