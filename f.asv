function y = f(p_guess,exhaust(1),exhaust(2),exhaust(3),strcoeff(1),strcoeff(2),strcoeff(3),deltav)
y = (c1*log((1+p_guess*c1)/(p_guess*c1*Str_Eff1)))+(c2*log((1+p_guess*c2)/(p_guess*c2*Str_Eff2))) - deltaV;
end
